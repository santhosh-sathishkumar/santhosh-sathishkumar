name: Generate Snake

# Regenerates the contribution-graph snake SVGs referenced by README.md
# Output branch: `output`
# Files:
#   - github-contribution-grid-snake.svg        (light)
#   - github-contribution-grid-snake-dark.svg   (dark)

on:
  schedule:
    - cron: "0 */24 * * *"   # once a day
  workflow_dispatch:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - name: Generate snake game from contribution graph
        uses: Platane/snk/svg-only@v3
        with:
          github_user_name: ${{ github.repository_owner }}
          outputs: |
            dist/github-contribution-grid-snake.svg
            dist/github-contribution-grid-snake-dark.svg?palette=github-dark

      - name: Push generated SVGs to `output` branch
        uses: crazy-max/ghaction-github-pages@v4
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

